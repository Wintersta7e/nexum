# Project B — memory subdir

Second form of CC memory layout: `<project>/memory/MEMORY.md`. Both forms must
be discoverable by the probe.

## Notes

- This file exists alongside the top-level CLAUDE.md, NOT instead of it.
- The probe should report both with separate file-type labels.
