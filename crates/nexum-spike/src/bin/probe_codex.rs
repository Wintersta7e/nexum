//! Phase 1a Codex probe — placeholder. Implementation lands in Task 5.

#![forbid(unsafe_code)]

fn main() {
    eprintln!("probe-codex: not implemented yet — see Phase 1a Task 5");
    std::process::exit(2);
}
