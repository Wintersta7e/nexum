//! Phase 1a CC probe — placeholder. Implementation lands in Task 4.

#![forbid(unsafe_code)]

fn main() {
    eprintln!("probe-cc: not implemented yet — see Phase 1a Task 4");
    std::process::exit(2);
}
